name: Release Build
run-name: >-
  ${{
    github.event_name == 'push'
    && format('Mobile Release {0}', github.ref_name)
    || format('Mobile Release v{0}', github.event.inputs.version)
  }}

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Version to build, for example 1.0.0"
        required: true
      source_ref:
        description: "liujin0506/codux ref to build, for example main, v1.8.1, or a full 40-character commit SHA"
        required: false
        default: main
      publish_release:
        description: "Publish GitHub Release assets after build"
        required: false
        default: false
        type: boolean
      log_level:
        description: "CODUX_LOG_LEVEL"
        required: false
        default: warn
        type: choice
        options:
          - debug
          - info
          - warn
          - error
          - off

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    concurrency:
      group: mobile-release-${{ github.ref_name || github.run_id }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          repository: liujin0506/codux
          ref: ${{ github.event_name == 'push' && github.ref_name || github.event.inputs.source_ref || 'main' }}
          token: ${{ secrets.CODUX_SOURCE_TOKEN }}

      - name: Set release working directory
        shell: bash
        run: echo "MOBILE_DIR=apps/mobile" >> "$GITHUB_ENV"

      - name: Set up Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Resolve build metadata
        id: meta
        shell: bash
        run: |
          version="${{ github.event.inputs.version }}"
          if [[ -z "${version}" ]]; then
            version="${GITHUB_REF_NAME}"
            version="${version#v}"
          fi
          if [[ -z "${version}" ]]; then
            echo "Release version is missing" >&2
            exit 1
          fi
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "build_number=${GITHUB_RUN_NUMBER}" >> "$GITHUB_OUTPUT"

      - name: Build release notes
        id: notes
        shell: bash
        run: |
          notes_path="$RUNNER_TEMP/release-notes.md"
          bash "$MOBILE_DIR/scripts/release/build-release-notes.sh" "${{ steps.meta.outputs.version }}" "$notes_path" "$MOBILE_DIR/CHANGELOG.md" "$MOBILE_DIR/CHANGELOG.zh-CN.md"
          echo "path=${notes_path}" >> "$GITHUB_OUTPUT"

      - name: Validate signing secrets
        if: github.event_name == 'push' || github.event.inputs.publish_release == 'true'
        shell: bash
        env:
          CODUX_ANDROID_KEYSTORE_BASE64: ${{ secrets.CODUX_ANDROID_KEYSTORE_BASE64 }}
          CODUX_ANDROID_KEYSTORE_PASSWORD: ${{ secrets.CODUX_ANDROID_KEYSTORE_PASSWORD }}
          CODUX_ANDROID_KEY_ALIAS: ${{ secrets.CODUX_ANDROID_KEY_ALIAS }}
          CODUX_ANDROID_KEY_PASSWORD: ${{ secrets.CODUX_ANDROID_KEY_PASSWORD }}
        run: |
          if [[ -z "${CODUX_ANDROID_KEYSTORE_BASE64:-}" || -z "${CODUX_ANDROID_KEYSTORE_PASSWORD:-}" || -z "${CODUX_ANDROID_KEY_ALIAS:-}" || -z "${CODUX_ANDROID_KEY_PASSWORD:-}" ]]; then
            echo "::warning::Android release signing secrets are not configured; publishing APK with debug signing fallback."
            exit 0
          fi

      - name: Configure Android signing
        shell: bash
        env:
          CODUX_ANDROID_KEYSTORE_BASE64: ${{ secrets.CODUX_ANDROID_KEYSTORE_BASE64 }}
          CODUX_ANDROID_KEYSTORE_PASSWORD: ${{ secrets.CODUX_ANDROID_KEYSTORE_PASSWORD }}
          CODUX_ANDROID_KEY_ALIAS: ${{ secrets.CODUX_ANDROID_KEY_ALIAS }}
          CODUX_ANDROID_KEY_PASSWORD: ${{ secrets.CODUX_ANDROID_KEY_PASSWORD }}
        run: |
          if [[ -z "${CODUX_ANDROID_KEYSTORE_BASE64:-}" ]]; then
            echo "[release] Android signing secrets not configured, using local debug signing fallback"
            exit 0
          fi
          keystore_path="$PWD/$MOBILE_DIR/android/app/codux-release.jks"
          printf '%s' "$CODUX_ANDROID_KEYSTORE_BASE64" | base64 --decode > "$keystore_path"
          {
            echo "storeFile=app/codux-release.jks"
            echo "storePassword=$CODUX_ANDROID_KEYSTORE_PASSWORD"
            echo "keyAlias=$CODUX_ANDROID_KEY_ALIAS"
            echo "keyPassword=$CODUX_ANDROID_KEY_PASSWORD"
          } > "$MOBILE_DIR/android/key.properties"

      - name: Install dependencies
        working-directory: apps/mobile
        run: flutter pub get

      - name: Analyze
        working-directory: apps/mobile
        run: flutter analyze

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: aarch64-linux-android

      - name: Restore Rust cache
        uses: actions/cache/restore@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: mobile-rust-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}-${{ github.run_id }}
          restore-keys: |
            mobile-rust-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}-
            mobile-rust-${{ runner.os }}-

      - name: Set up Zig
        uses: mlugg/setup-zig@v2
        with:
          version: 0.15.2

      - name: Build Rust FFI for host tests
        shell: bash
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ runner.temp }}/zig-global-cache-host
          ZIG_LOCAL_CACHE_DIR: ${{ runner.temp }}/zig-local-cache-host
        run: |
          rm -rf "$ZIG_GLOBAL_CACHE_DIR" "$ZIG_LOCAL_CACHE_DIR"
          mkdir -p "$ZIG_GLOBAL_CACHE_DIR" "$ZIG_LOCAL_CACHE_DIR"
          cargo build -p codux-protocol-ffi --release
          echo "LD_LIBRARY_PATH=$PWD/target/release:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"

      - name: Test
        working-directory: apps/mobile
        run: flutter test

      - name: Install Android NDK
        shell: bash
        env:
          ANDROID_NDK_VERSION: "28.2.13676358"
        run: |
          sdkmanager_bin=""
          for candidate in \
            "${ANDROID_HOME:-}/cmdline-tools/latest/bin/sdkmanager" \
            "${ANDROID_SDK_ROOT:-}/cmdline-tools/latest/bin/sdkmanager" \
            "${ANDROID_HOME:-}/tools/bin/sdkmanager" \
            "${ANDROID_SDK_ROOT:-}/tools/bin/sdkmanager"; do
            if [[ -x "$candidate" ]]; then
              sdkmanager_bin="$candidate"
              break
            fi
          done
          if [[ -z "$sdkmanager_bin" ]]; then
            echo "sdkmanager was not found under ANDROID_HOME=${ANDROID_HOME:-} or ANDROID_SDK_ROOT=${ANDROID_SDK_ROOT:-}" >&2
            exit 1
          fi
          set +o pipefail
          yes | "$sdkmanager_bin" "ndk;${ANDROID_NDK_VERSION}"
          sdkmanager_status="${PIPESTATUS[1]}"
          set -o pipefail
          if [[ "$sdkmanager_status" -ne 0 ]]; then
            exit "$sdkmanager_status"
          fi
          android_sdk_root="${ANDROID_HOME:-${ANDROID_SDK_ROOT}}"
          ndk_path="${android_sdk_root}/ndk/${ANDROID_NDK_VERSION}"
          echo "ANDROID_NDK_HOME=${ndk_path}" >> "$GITHUB_ENV"
          echo "ANDROID_NDK_ROOT=${ndk_path}" >> "$GITHUB_ENV"
          if [[ -d "${android_sdk_root}/ndk" ]]; then
            find "${android_sdk_root}/ndk" -mindepth 1 -maxdepth 1 -type d ! -name "${ANDROID_NDK_VERSION}" -exec rm -rf {} +
          fi

      - name: Install cargo-ndk
        uses: taiki-e/install-action@v2
        with:
          tool: cargo-ndk

      - name: Build Rust FFI for Android
        shell: bash
        env:
          ZIG_GLOBAL_CACHE_DIR: ${{ runner.temp }}/zig-global-cache-android
          ZIG_LOCAL_CACHE_DIR: ${{ runner.temp }}/zig-local-cache-android
        run: |
          rm -rf "$ZIG_GLOBAL_CACHE_DIR" "$ZIG_LOCAL_CACHE_DIR"
          mkdir -p "$ZIG_GLOBAL_CACHE_DIR" "$ZIG_LOCAL_CACHE_DIR"
          bash "$MOBILE_DIR/plugin/codux_protocol_ffi/scripts/build-android.sh"
          echo "CODUX_SKIP_ANDROID_FFI_BUILD=1" >> "$GITHUB_ENV"

      # Saved before Free disk space, which deletes the target directories the
      # next run wants to restore.
      - name: Save Rust cache
        if: always()
        continue-on-error: true
        uses: actions/cache/save@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            target
          key: mobile-rust-${{ runner.os }}-${{ hashFiles('Cargo.lock') }}-${{ github.run_id }}

      - name: Restore Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: mobile-gradle-${{ runner.os }}-${{ hashFiles('apps/mobile/android/**/*.gradle*', 'apps/mobile/android/gradle/wrapper/gradle-wrapper.properties') }}
          restore-keys: |
            mobile-gradle-${{ runner.os }}-

      - name: Free disk space
        shell: bash
        run: |
          df -h /
          rm -rf target/debug target/release
          rm -rf "${{ runner.temp }}/zig-global-cache-host" "${{ runner.temp }}/zig-local-cache-host"
          sudo rm -rf /usr/share/dotnet /opt/ghc /usr/local/share/boost || true
          df -h /

      - name: Build release APK
        working-directory: apps/mobile
        shell: bash
        env:
          GRADLE_OPTS: -Dorg.gradle.vfs.watch=false -Dorg.gradle.daemon=false
        run: |
          rm -rf dist
          mkdir -p dist
          flutter build apk --release --target-platform android-arm64 \
            --build-name "${{ steps.meta.outputs.version }}" \
            --build-number "${{ steps.meta.outputs.build_number }}" \
            --dart-define "CODUX_LOG_LEVEL=${{ github.event.inputs.log_level || 'warn' }}"
          cp build/app/outputs/flutter-apk/app-release.apk "dist/Codux-Mobile-${{ steps.meta.outputs.version }}-arm64-v8a-android.apk"
          (cd dist && sha256sum *.apk > SHA256SUMS.txt)
          # Version-less alias so the website can link releases/latest/download/codux-android.apk
          cp build/app/outputs/flutter-apk/app-release.apk "dist/codux-android.apk"

      - name: Upload workflow artifact
        uses: actions/upload-artifact@v4
        with:
          name: codux-mobile-${{ steps.meta.outputs.version }}-arm64-v8a-android
          path: |
            apps/mobile/dist/Codux-Mobile-${{ steps.meta.outputs.version }}-arm64-v8a-android.apk
            apps/mobile/dist/SHA256SUMS.txt
          if-no-files-found: error

      - name: Upload release assets
        if: github.event_name == 'push' || github.event.inputs.publish_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ steps.meta.outputs.version }}
          name: Codux Mobile v${{ steps.meta.outputs.version }}
          body_path: ${{ steps.notes.outputs.path }}
          files: |
            apps/mobile/dist/Codux-Mobile-${{ steps.meta.outputs.version }}-arm64-v8a-android.apk
            apps/mobile/dist/codux-android.apk
            apps/mobile/dist/SHA256SUMS.txt
